booking:
  property: Product Profile Dev

spring:
  application:
    name: product-microservice

server:
  port: 0

eureka:
  instance:
    instance-id: ${spring.application.name}:${random.uuid}

app:
  testProp: 4
